//Postponed until tower class wants to implement ariel attacks

/*class flyer: Enemy {
    //standard speed, health, isFlying
    #define MAX_HEALTH 100
    #define SPEED 3
    #define REWARD 100

    override init() { init(x: 0, y: 0) }

    override init(x: Int, y: Int){
        super.init(x: x, y: y, speed: SPEED, hpMax: MAX_HEALTH)
    }
}*/